import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const workspaces = sqliteTable('workspaces', { id: text('id').primaryKey(), policy: text('policy').notNull(), created: text('created').notNull() });
export const transactions = sqliteTable('transactions', { id: text('id').primaryKey(), workspace: text('workspace').notNull(), runId: text('run_id').notNull(), scenario: text('scenario').notNull(), invoice: text('invoice').notNull(), amount: integer('amount').notNull(), status: text('status').notNull(), evidence: text('evidence').notNull(), assessment: text('assessment').notNull(), trace: text('trace').notNull(), verification: text('verification'), created: text('created').notNull(), updated: text('updated').notNull() }, t => [index('idx_transactions_workspace_created').on(t.workspace, t.created)]);
export const evaluations = sqliteTable('evaluations', { id: text('id').primaryKey(), workspace: text('workspace').notNull(), result: text('result').notNull(), created: text('created').notNull() }, t => [index('idx_evaluations_workspace_created').on(t.workspace, t.created)]);
